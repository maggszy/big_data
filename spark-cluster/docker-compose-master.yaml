services:
  spark-master:
    container_name: spark-master
    build: .
    volumes:
      - './src/:/opt/app/src'
    ports:
      - "7077:7077"
      - "8080:8080"
    env-file: ".env"
    entrypoint: ["sh", "/opt/app/entrypoints/master-entrypoint.sh"]